version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27018:27017"


  app:
    build: .
    container_name: app
    ports:
      - "8000:8000"
    depends_on:
      - mongo